# Commit and Documentation Walkthrough

## 実施内容

コミット前にプロジェクトの `README.md` を更新し、最近追加された主要機能を英日両言語で記述しました。その後、指定された詳細な内容を含むコミットメッセージで変更をコミットしました。

## 変更点

### 1. README.md の更新

- **英日対応**: 英語と日本語の両方で解説を記載。
- **絵文字の削除**: ユーザーの依頼に基づき、すべての絵文字を削除し、プロフェッショナルなスタイルに統一。
- **新機能の網羅**:
  - 通知領域の制御と、録音中の通知固定。
  - 4つのVRAM管理モード（Local, CPU Offload, Timed VRAM Retention, Resident VRAM）。
  - GUIによるモード選択。

### 2. コミットの実行

- **コミットメッセージ**: 以下の内容を日本語と英語の両方で記載しました。
  - 通知制御、録音中通知の改善。
  - ローカル読み込み、CPUオフロードのサポート。
  - GUIによる4つの動作モード選択。

## 検証結果

- [x] `README.md` が英日両対応・絵文字なしで更新されていることを確認。
- [x] `git status` がクリアになり、コミットが正常に完了したことを確認。
- [x] コミットメッセージに指定されたすべての項目が含まれていることを確認。

## 次のステップ

- **句読点オプションの実装**: 本来の依頼である「句読点を打つオプション」の追加に着手します。
